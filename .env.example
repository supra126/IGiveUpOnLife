# Gemini API Key (optional - if set, users can use the app without their own key)
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Gemini Text Model (optional - default: gemini-2.5-flash)
# Options: gemini-2.5-flash, gemini-2.5-pro
# - gemini-2.5-flash: Fast and cost-effective (recommended for most cases)
# - gemini-2.5-pro: Better reasoning, higher quality output (10x cost)
GEMINI_TEXT_MODEL=gemini-2.5-flash

# Rate Limiting (only applies when using server API key)
# Time window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Max requests per window (default: 10)
RATE_LIMIT_MAX_REQUESTS=10
# Enable/disable rate limiting (default: true)
RATE_LIMIT_ENABLED=true
